{% extends 'base.html.twig' %}


{% block body %}
    {% for entreprise in entreprises %}
        {% if entreprise.stages|length != NULL %}
            <a href="{{path('pro_stages_entreprises_trie',{id:entreprise.id})}}"><p>{{entreprise.nom}}</p></a>
            <br>
        {% endif %}
    {% endfor %}
{% endblock %}
